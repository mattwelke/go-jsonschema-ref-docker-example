{
    "$id": "pet",
    "title": "Pet",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The pet's first name.",
        "minLength": 1,
        "maxLength": 200
      }
    }
  }